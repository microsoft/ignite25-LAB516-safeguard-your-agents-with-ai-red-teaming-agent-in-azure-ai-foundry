# Azure AI Project Configuration
# Required: The endpoint URL for your Azure AI Project
# Example: "https://aoai-XXXXX.services.ai.azure.com/api/projects/proj-XXXXX"
AZURE_EXISTING_AIPROJECT_ENDPOINT=

# Agent Configuration
# Option 1: Specify the agent by ID (preferred if you know the ID)
AZURE_EXISTING_AGENT_ID=

# Option 2: Specify the agent by name (the script will look it up)
# Example: "agent-template-assistant" (default)
AZURE_AI_AGENT_NAME=

# Optional: Model deployment name
# If not specified, the script will use the model from the agent configuration
# Example: gpt-4.1
AZURE_AI_AGENT_DEPLOYMENT_NAME=

# Required: For Lab 02
AZURE_SUBSCRIPTION_ID=
AZURE_RESOURCE_GROUP=
AZURE_AIPROJECT_NAME=
AZURE_AI_PROJECT=


# Azure Authentication
# The script uses DefaultAzureCredential which supports multiple authentication methods:
# - Azure CLI: Run 'az login' before running the script
# - Environment variables: Set AZURE_TENANT_ID, AZURE_CLIENT_ID, AZURE_CLIENT_SECRET
# - Managed Identity: Available when running on Azure services
# - Visual Studio Code: Sign in through VS Code Azure extension
# - Interactive browser: Will prompt for login if other methods fail
